# Invalid Entries Quarantine

Files in this directory are memory entries that failed validation (unparseable frontmatter, missing required fields, etc.).

**Phase 0**: Entries are not automatically moved here. Agents should report invalid entries with a warning and skip them. Manual quarantine may be done by the user.

Do not read files from this directory for normal recall.
